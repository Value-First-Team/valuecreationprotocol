# Keep dependencies local — do not hoist to parent directories.
# The next/ subdirectory is a self-contained project nested inside the
# Astro site's repo. Hoisting would resolve @vf/site-kit from the parent's
# Astro-era node_modules (v0.1, no React exports) instead of this dir's v0.2.
install-strategy=linked
